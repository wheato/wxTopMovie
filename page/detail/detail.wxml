
<view class="detail-wrap">
    <loading hidden="{{hidden}}">
        加载中...
    </loading>
    <image mode="aspectFit" src="{{movie.image}}" class="detail-movie-cover"></image>
    <view>{{movie.title}}</view>
    <view wx:if="{{movie.average}}">评分: {{movie.average}}</view>
</view>